

@if (TempData["Message"] != null)
{
    <h5 class="alert alert-info">@TempData["Message"]</h5>
}

@if (Model.Count == 0)
{
    <h2 class="alert alert-danger">No student in DB. Add student first !</h2>
    <a class="btn btn-success" asp-controller="Student" asp-action="Add">
        Add
    </a>
}
else
{
    <div class="container mt-3 text-center">
        <table class="table-bordered text-center mt-3 mb-3 bg-light">
            <tr>
                <th class="h3 text-primary" colspan="4">Student List</th>
                <th>
                    <a class="btn btn-success" asp-controller="Student" asp-action="Add">
                        Add
                    </a>
                </th>
            </tr>
            <tr>
                <th>Admin Id</th>
                <th>Admin Name</th>
            </tr>
            @*@foreach (var admin in Model)
            {
                <tr>
                    <td>@admin.Id</td>
                    <td>@admin.Ad_name</td>
                    <td>
                        <a asp-controller="Admin" asp-action="Info" asp-route-id="@admin.Id"></a>
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Admin" asp-action="Update" asp-route-id="@admin.Id">
                            Edit
                        </a>*@
                        @*@if (User.Identity.IsAuthenticated && User.IsInRole("Administrator"))*@
                        @*{*@
                           @* <a class="btn btn-danger" asp-controller="Admin" asp-action="Delete" asp-route-id="@admin.Id"
                                onclick="return confirm('Are you sure to delete this student?');">
                                Delete
                            </a>*@
                        @*}*@

             @*       </td>
                </tr>
            }*@
        </table>
    </div>
}